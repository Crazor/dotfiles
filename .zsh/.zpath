#!/bin/zsh
# Make path's entries unique
typeset -U path

if [[ -d /opt/local/bin ]]; then
	path=(/opt/local/bin $path)
fi
if [[ -d /opt/local/sbin ]]; then
	path=(/opt/local/sbin $path)
fi
if [[ -d ~/bin ]]; then
	path=(~/bin $path)
fi
