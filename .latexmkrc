# Immer PDFs erzeugen (direkt mit pdflatex)
$pdf_mode = 1;

# Bei Fehlern nicht anhalten
$pdflatex_silent_switch = "-interaction=nonstopmode";

# PDFs mit Skim öffnen
$pdf_previewer = "open -a Skim";

# vim: syntax=perl
